all-local:
	cp -dR --no-preserve=mode --preserve=timestamps --no-target-directory --update --reflink=auto $(srcdir)/daala_tools.upstream ./daala_tools && cd daala_tools && $(MAKE) $(AM_MAKEFLAGS)

check-local:

clean-local:
	rm -rf daala_tools

install-data-local:

install-exec-local:

uninstall-local:

EXTRA_DIST = daala_tools.upstream
